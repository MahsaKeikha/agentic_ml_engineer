# Architecture

`case -> ML Engineering Orchestrator -> Data Assessment -> Modeling Strategy -> Evaluation -> Reproducibility Auditor -> Deployment Handoff -> human gate`

The shared state preserves dataset/model context, evaluation evidence, reproducibility metadata, risks, conflicts, and the full execution trace.
